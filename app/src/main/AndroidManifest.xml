<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permisos que necesita la app -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/mis_medicinas"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ControlMedicamentos"
        tools:targetApi="31">

        <!-- Pantalla de login - Actividad principal -->
        <activity
            android:name=".LoginActivity"
            android:exported="true"
            android:theme="@style/Theme.ControlMedicamentos">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Pantalla principal -->
        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:theme="@style/Theme.ControlMedicamentos" />

        <!-- Pantalla botiquín -->
        <activity
            android:name=".BotiquinActivity"
            android:exported="false"
            android:theme="@style/Theme.ControlMedicamentos" />

        <!-- Pantalla nueva medicina -->
        <activity
            android:name=".NuevaMedicinaActivity"
            android:exported="false"
            android:theme="@style/Theme.ControlMedicamentos" />

        <!-- Pantalla historial -->
        <activity
            android:name=".HistorialActivity"
            android:exported="false"
            android:theme="@style/Theme.ControlMedicamentos" />

        <!-- Pantalla ajustes -->
        <activity
            android:name=".AjustesActivity"
            android:exported="false"
            android:theme="@style/Theme.ControlMedicamentos" />

        <!-- Activity para manejar callback de Google Calendar OAuth -->
        <activity
            android:name=".GoogleCalendarCallbackActivity"
            android:exported="true"
            android:theme="@style/Theme.ControlMedicamentos">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="com.controlmedicamentos.myapplication" 
                      android:host="googlecalendar" />
            </intent-filter>
        </activity>

        <!-- Receiver para alarmas de medicamentos -->
        <receiver
            android:name=".receivers.AlarmReceiver"
            android:exported="false"
            android:enabled="true" />

        <!-- Receiver para reiniciar alarmas cuando el dispositivo se reinicia -->
        <receiver
            android:name=".receivers.BootReceiver"
            android:exported="true"
            android:enabled="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </receiver>

    </application>

</manifest>